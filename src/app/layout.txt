          <nav className="flex items-center justify-between pb-8 pt-8 w-full py-0 px-24 m-auto sticky top-0 bg-white z-10">
            <h1 className="font-extrabold text-3xl">Ulcer Detect</h1>
            <div>
              {!(await isAuthenticated()) ? (
                <>
                  <LoginLink>
                    <Button variant="default">Sign in</Button>
                  </LoginLink>
                  <RegisterLink>
                    <Button variant="outline">Sign up </Button>
                    </RegisterLink>
                </>
              ) : (
                <div className="items-center grid grid-cols-1 gap-2">
                  {user?.picture ? (
                    <img
                      className="items-center bg-gray-100 rounded-full flex justify-center text-center w-12 h-12"
                      src={user?.picture}
                      alt="user profile avatar"
                      referrerPolicy="no-referrer"
                    />
                  ) : (
                    <div className="items-center bg-gray-100 rounded-lg flex justify-center text-center w-12 h-12">
                      {user?.given_name?.[0]}
                      {user?.family_name?.[0]}
                    </div>
                  )}
                  <div>
                    <p className="text-heading-2">
                      {user?.given_name} {user?.family_name}
                    </p>
                    <LogoutLink >
                      <Button>Log out </Button>
                    </LogoutLink>
                  </div>
                </div>
              )}
            </div>
          </nav>
   